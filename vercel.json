{
  "installCommand": "if [ -d frontend ]; then cd frontend; fi; npm install",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build && cd .. && rm -rf dist && cp -R frontend/dist dist; else npm run build && rm -rf ../dist && cp -R dist ../dist; fi",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
