// Temporary in-repo fallback for deployments where Vite env vars are not set.
// Replace these with your real Supabase project values only if you explicitly
// want to keep config in code.
export const SUPABASE_URL_FALLBACK = "";
export const SUPABASE_ANON_KEY_FALLBACK = "";

